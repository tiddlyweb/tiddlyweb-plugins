
A version of paste's <http://pythonpaste.org/> gzipper middleware
which takes an output stream and gzip's before sending it on to
the browser.

To use add 'gzipper' to system_plugins in tiddlywebconfig.py.

NOTE: This plugin has seen very little testing. It is unknown if
it will interact properly with the various caching strategies 
presented by some plugins.
